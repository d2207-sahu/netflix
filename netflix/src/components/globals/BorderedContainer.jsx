import styled from 'styled-components'

// TODO change the color to something else
const BorderedContainer = styled.div`
    border: 1px solid hsla(0,0%,100%,.4);
    border-radius: 3px;
    color: hsla(0,0%,100%,.9);
    font-size: .7em;
    padding: 0 0.5em;
    white-space: nowrap;
    width: fit-content;
`

export default BorderedContainer